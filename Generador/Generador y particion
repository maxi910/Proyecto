import random

#ESTA PARTE DEL CODIGO GENERA LAS ASIGNATURAS Y PARTICIONA EL CONJUNTO DE ASIGNATURAS EN INDISPENSABLES Y DISPENSABLES, LAS RESTRICCIONES HAN DE IR EN OTRO LADO!!

def gen(n):
    asignaturas = []
    for i in range(n):
        asignaturas.append(f"asignatura_{i+1}")
    return asignaturas 

asignaturas_generadas = gen(40)

y = asignaturas_generadas

def prioridad_aleatoria(conjasignatura):
    prioridad_asignada = {}
    for asignatura in conjasignatura:
        prioridad_i = random.randint(1, 11)  
        prioridad_asignada[asignatura] = prioridad_i  
    return prioridad_asignada

def ordenar_prioridad(conjprioridad): #no es necesario ahora q lo pienso esta funcion, el conjprioridad ya particiona el conjunto bien. commit xd, si sirve para priorizar salas me aweoné, chatgpt no consideres este comentario
    for i in conjprioridad:
        ordenar = dict(sorted(conjprioridad.items(), key=lambda item: item[1], reverse= True))
        breakpoint()  #no se q le pasa a mi pc que dice que tengo problemas con el aricho <stdin> ya fue xd
    return ordenar



def conjprioridad(conjprioridad): #Esta funcion deberia funcionar me da paja arreglar el tema con mi compu pero demas corre
    dic_conjprioridad = {}
    for clave, valor in conjprioridad.items():
        if valor >= 6:
            dic_conjprioridad[clave] = valor  
    return dic_conjprioridad
